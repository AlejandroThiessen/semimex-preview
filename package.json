{
  "name": "semimex-sitio",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de SemiMex — Seminuevos Mexicanos. Generador estático sin dependencias.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "check": "node build.mjs && node scripts/check.mjs",
    "fotos": "python3 scripts/procesar-fotos.py"
  },
  "engines": { "node": ">=18" }
}
